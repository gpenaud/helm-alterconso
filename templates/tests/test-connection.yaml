apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cagette.fullname" . }}-test-connection"
  labels:
    {{- include "cagette.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cagette.fullname" . }}:{{ .Values.app.service.port }}']
  restartPolicy: Never
